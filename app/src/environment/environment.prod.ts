export const environment = {
  production: true,
  // In development, point to the Netlify dev server or local backend
  // When deployed to Netlify, use relative paths (empty string) since functions are on same domain
  apiBaseUrl: ''
};
